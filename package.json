{
  "name": "clean-node-api",
  "version": "1.0.0",
  "description": "Estudo de construção de api Rest node js",
  "main": "index.js",
  "scripts": {
    "test": "jest  --colors --noStackTrace --passWithNoTests",
    "test:unit": "yarn test == --watchAll -c jest-unit-config.js",
    "test:integration": "yarn  test == --watch  -c jest-integration-config.js",
    "test:staged": "yarn  test ==  --findRelatedTests",
    "test:ci": "jest  --coverage --colors --noStackTrace --passWithNoTests"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gemirson/clean-node-api.git"
  },
  "author": "Gemirson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/gemirson/clean-node-api/issues"
  },
  "homepage": "https://github.com/gemirson/clean-node-api#readme",
  "devDependencies": {
    "husky": "^4.2.5",
    "jest": "^26.1.0",
    "jest-cli": "^26.1.0",
    "lint-staged": "^10.2.11",
    "standard": "^14.3.4"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongose": "^0.0.2-security",
    "validator": "^13.1.1"
  }
}
