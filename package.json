{
  "name": "clean-node-api",
  "version": "1.0.0",
  "description": "Estudo de construção de api Rest node js",
  "main": "src/main/index.js",
  "scripts": {
    "start": "node src/main/index.js",
    "test": "jest  --colors  --passWithNoTests",
    "test:unit": "jest --colors --watch --passWithNoTests -c jest-unit-config.js --forceExit",
    "test:integration": "jest --colors --noStackTrace --passWithNoTests  -c jest-integration-config.js --forceExit",
    "test:staged": "jest --colors --runInBand --detectOpenHandles  --passWithNoTests --forceExit ",
    "test:ci": "jest  --coverage --colors --noStackTrace --passWithNoTests --forceExit"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gemirson/clean-node-api.git"
  },
  "author": "Gemirson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/gemirson/clean-node-api/issues"
  },
  "homepage": "https://github.com/gemirson/clean-node-api#readme",
  "devDependencies": {
    "@shelf/jest-mongodb": "^1.2.1",
    "husky": "^4.2.5",
    "jest": "^26.1.0",
    "jest-cli": "^26.1.0",
    "lint-staged": "^10.2.11",
    "standard": "^14.3.4",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "bcrypt": "^5.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^3.5.9",
    "mongose": "^0.0.2-security",
    "validator": "^13.1.1"
  }
}
